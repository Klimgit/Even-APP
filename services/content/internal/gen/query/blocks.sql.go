// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: blocks.sql

package query

import (
	"context"

	"github.com/google/uuid"
)

const createBlock = `-- name: CreateBlock :one
INSERT INTO lesson_blocks (lesson_id, section_id, sort_order, display_label, title, block_type, config, is_homework)
VALUES ($1, $2, $3, $4, $5, $6, $7, $8)
RETURNING id, lesson_id, section_id, sort_order, display_label, title, block_type, config, is_homework
`

type CreateBlockParams struct {
	LessonID     uuid.UUID
	SectionID    *uuid.UUID
	SortOrder    int32
	DisplayLabel *string
	Title        *string
	BlockType    string
	Config       []byte
	IsHomework   bool
}

// CreateBlock
//
//	INSERT INTO lesson_blocks (lesson_id, section_id, sort_order, display_label, title, block_type, config, is_homework)
//	VALUES ($1, $2, $3, $4, $5, $6, $7, $8)
//	RETURNING id, lesson_id, section_id, sort_order, display_label, title, block_type, config, is_homework
func (q *Queries) CreateBlock(ctx context.Context, arg CreateBlockParams) (LessonBlock, error) {
	row := q.db.QueryRow(ctx, createBlock,
		arg.LessonID,
		arg.SectionID,
		arg.SortOrder,
		arg.DisplayLabel,
		arg.Title,
		arg.BlockType,
		arg.Config,
		arg.IsHomework,
	)
	var i LessonBlock
	err := row.Scan(
		&i.ID,
		&i.LessonID,
		&i.SectionID,
		&i.SortOrder,
		&i.DisplayLabel,
		&i.Title,
		&i.BlockType,
		&i.Config,
		&i.IsHomework,
	)
	return i, err
}

const deleteBlock = `-- name: DeleteBlock :exec
DELETE FROM lesson_blocks WHERE id = $1
`

// DeleteBlock
//
//	DELETE FROM lesson_blocks WHERE id = $1
func (q *Queries) DeleteBlock(ctx context.Context, id uuid.UUID) error {
	_, err := q.db.Exec(ctx, deleteBlock, id)
	return err
}

const getBlockByID = `-- name: GetBlockByID :one
SELECT id, lesson_id, section_id, sort_order, display_label, title, block_type, config, is_homework
FROM lesson_blocks
WHERE id = $1
`

// GetBlockByID
//
//	SELECT id, lesson_id, section_id, sort_order, display_label, title, block_type, config, is_homework
//	FROM lesson_blocks
//	WHERE id = $1
func (q *Queries) GetBlockByID(ctx context.Context, id uuid.UUID) (LessonBlock, error) {
	row := q.db.QueryRow(ctx, getBlockByID, id)
	var i LessonBlock
	err := row.Scan(
		&i.ID,
		&i.LessonID,
		&i.SectionID,
		&i.SortOrder,
		&i.DisplayLabel,
		&i.Title,
		&i.BlockType,
		&i.Config,
		&i.IsHomework,
	)
	return i, err
}

const getBlockCourseOwner = `-- name: GetBlockCourseOwner :one
SELECT c.owner_id
FROM lesson_blocks b
JOIN lessons l ON l.id = b.lesson_id
JOIN courses c ON c.id = l.course_id
WHERE b.id = $1
`

// GetBlockCourseOwner
//
//	SELECT c.owner_id
//	FROM lesson_blocks b
//	JOIN lessons l ON l.id = b.lesson_id
//	JOIN courses c ON c.id = l.course_id
//	WHERE b.id = $1
func (q *Queries) GetBlockCourseOwner(ctx context.Context, id uuid.UUID) (uuid.UUID, error) {
	row := q.db.QueryRow(ctx, getBlockCourseOwner, id)
	var owner_id uuid.UUID
	err := row.Scan(&owner_id)
	return owner_id, err
}

const listBlocksByCourseID = `-- name: ListBlocksByCourseID :many
SELECT b.id, b.lesson_id, b.section_id, b.sort_order, b.display_label, b.title, b.block_type, b.config, b.is_homework,
       l.title AS lesson_title, l.sort_order AS lesson_sort_order
FROM lesson_blocks b
JOIN lessons l ON l.id = b.lesson_id
WHERE l.course_id = $1
ORDER BY l.sort_order, b.sort_order, b.id
`

type ListBlocksByCourseIDRow struct {
	ID              uuid.UUID
	LessonID        uuid.UUID
	SectionID       *uuid.UUID
	SortOrder       int32
	DisplayLabel    *string
	Title           *string
	BlockType       string
	Config          []byte
	IsHomework      bool
	LessonTitle     string
	LessonSortOrder int32
}

// ListBlocksByCourseID
//
//	SELECT b.id, b.lesson_id, b.section_id, b.sort_order, b.display_label, b.title, b.block_type, b.config, b.is_homework,
//	       l.title AS lesson_title, l.sort_order AS lesson_sort_order
//	FROM lesson_blocks b
//	JOIN lessons l ON l.id = b.lesson_id
//	WHERE l.course_id = $1
//	ORDER BY l.sort_order, b.sort_order, b.id
func (q *Queries) ListBlocksByCourseID(ctx context.Context, courseID uuid.UUID) ([]ListBlocksByCourseIDRow, error) {
	rows, err := q.db.Query(ctx, listBlocksByCourseID, courseID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListBlocksByCourseIDRow
	for rows.Next() {
		var i ListBlocksByCourseIDRow
		if err := rows.Scan(
			&i.ID,
			&i.LessonID,
			&i.SectionID,
			&i.SortOrder,
			&i.DisplayLabel,
			&i.Title,
			&i.BlockType,
			&i.Config,
			&i.IsHomework,
			&i.LessonTitle,
			&i.LessonSortOrder,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listBlocksByLessonID = `-- name: ListBlocksByLessonID :many
SELECT id, lesson_id, section_id, sort_order, display_label, title, block_type, config, is_homework
FROM lesson_blocks
WHERE lesson_id = $1
ORDER BY sort_order, id
`

// ListBlocksByLessonID
//
//	SELECT id, lesson_id, section_id, sort_order, display_label, title, block_type, config, is_homework
//	FROM lesson_blocks
//	WHERE lesson_id = $1
//	ORDER BY sort_order, id
func (q *Queries) ListBlocksByLessonID(ctx context.Context, lessonID uuid.UUID) ([]LessonBlock, error) {
	rows, err := q.db.Query(ctx, listBlocksByLessonID, lessonID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []LessonBlock
	for rows.Next() {
		var i LessonBlock
		if err := rows.Scan(
			&i.ID,
			&i.LessonID,
			&i.SectionID,
			&i.SortOrder,
			&i.DisplayLabel,
			&i.Title,
			&i.BlockType,
			&i.Config,
			&i.IsHomework,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const maxBlockSortOrder = `-- name: MaxBlockSortOrder :one
SELECT COALESCE(MAX(sort_order), -1)::int AS max_sort
FROM lesson_blocks
WHERE lesson_id = $1
`

// MaxBlockSortOrder
//
//	SELECT COALESCE(MAX(sort_order), -1)::int AS max_sort
//	FROM lesson_blocks
//	WHERE lesson_id = $1
func (q *Queries) MaxBlockSortOrder(ctx context.Context, lessonID uuid.UUID) (int32, error) {
	row := q.db.QueryRow(ctx, maxBlockSortOrder, lessonID)
	var max_sort int32
	err := row.Scan(&max_sort)
	return max_sort, err
}

const setBlockSortOrder = `-- name: SetBlockSortOrder :exec
UPDATE lesson_blocks SET sort_order = $2 WHERE id = $1
`

type SetBlockSortOrderParams struct {
	ID        uuid.UUID
	SortOrder int32
}

// SetBlockSortOrder
//
//	UPDATE lesson_blocks SET sort_order = $2 WHERE id = $1
func (q *Queries) SetBlockSortOrder(ctx context.Context, arg SetBlockSortOrderParams) error {
	_, err := q.db.Exec(ctx, setBlockSortOrder, arg.ID, arg.SortOrder)
	return err
}

const updateBlock = `-- name: UpdateBlock :one
UPDATE lesson_blocks
SET
    section_id = COALESCE($1, section_id),
    sort_order = COALESCE($2, sort_order),
    display_label = COALESCE($3, display_label),
    title = COALESCE($4, title),
    block_type = COALESCE($5, block_type),
    config = COALESCE($6, config),
    is_homework = COALESCE($7, is_homework)
WHERE id = $8
RETURNING id, lesson_id, section_id, sort_order, display_label, title, block_type, config, is_homework
`

type UpdateBlockParams struct {
	SectionID    *uuid.UUID
	SortOrder    *int32
	DisplayLabel *string
	Title        *string
	BlockType    *string
	Config       []byte
	IsHomework   *bool
	ID           uuid.UUID
}

// UpdateBlock
//
//	UPDATE lesson_blocks
//	SET
//	    section_id = COALESCE($1, section_id),
//	    sort_order = COALESCE($2, sort_order),
//	    display_label = COALESCE($3, display_label),
//	    title = COALESCE($4, title),
//	    block_type = COALESCE($5, block_type),
//	    config = COALESCE($6, config),
//	    is_homework = COALESCE($7, is_homework)
//	WHERE id = $8
//	RETURNING id, lesson_id, section_id, sort_order, display_label, title, block_type, config, is_homework
func (q *Queries) UpdateBlock(ctx context.Context, arg UpdateBlockParams) (LessonBlock, error) {
	row := q.db.QueryRow(ctx, updateBlock,
		arg.SectionID,
		arg.SortOrder,
		arg.DisplayLabel,
		arg.Title,
		arg.BlockType,
		arg.Config,
		arg.IsHomework,
		arg.ID,
	)
	var i LessonBlock
	err := row.Scan(
		&i.ID,
		&i.LessonID,
		&i.SectionID,
		&i.SortOrder,
		&i.DisplayLabel,
		&i.Title,
		&i.BlockType,
		&i.Config,
		&i.IsHomework,
	)
	return i, err
}
