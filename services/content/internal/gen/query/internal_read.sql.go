// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: internal_read.sql

package query

import (
	"context"

	"github.com/google/uuid"
)

const countPublishedCourses = `-- name: CountPublishedCourses :one
SELECT COUNT(*)::int AS count FROM courses WHERE is_published = true
`

// CountPublishedCourses
//
//	SELECT COUNT(*)::int AS count FROM courses WHERE is_published = true
func (q *Queries) CountPublishedCourses(ctx context.Context) (int32, error) {
	row := q.db.QueryRow(ctx, countPublishedCourses)
	var count int32
	err := row.Scan(&count)
	return count, err
}

const getCourseByInviteCode = `-- name: GetCourseByInviteCode :one

SELECT
    c.id,
    c.title,
    c.target_language_id,
    ''::text AS target_language_code,
    ''::text AS target_language_name,
    c.ui_language_id,
    c.owner_id,
    c.is_published,
    c.visibility,
    ic.code AS invite_code
FROM courses c
JOIN course_invite_codes ic ON ic.course_id = c.id
WHERE ic.code = $1 AND c.is_published = true
`

type GetCourseByInviteCodeRow struct {
	ID                 uuid.UUID
	Title              string
	TargetLanguageID   uuid.UUID
	TargetLanguageCode string
	TargetLanguageName string
	UiLanguageID       uuid.UUID
	OwnerID            uuid.UUID
	IsPublished        bool
	Visibility         string
	InviteCode         string
}

// Internal read queries for service-to-service HTTP handlers.
//
//	SELECT
//	    c.id,
//	    c.title,
//	    c.target_language_id,
//	    ''::text AS target_language_code,
//	    ''::text AS target_language_name,
//	    c.ui_language_id,
//	    c.owner_id,
//	    c.is_published,
//	    c.visibility,
//	    ic.code AS invite_code
//	FROM courses c
//	JOIN course_invite_codes ic ON ic.course_id = c.id
//	WHERE ic.code = $1 AND c.is_published = true
func (q *Queries) GetCourseByInviteCode(ctx context.Context, code string) (GetCourseByInviteCodeRow, error) {
	row := q.db.QueryRow(ctx, getCourseByInviteCode, code)
	var i GetCourseByInviteCodeRow
	err := row.Scan(
		&i.ID,
		&i.Title,
		&i.TargetLanguageID,
		&i.TargetLanguageCode,
		&i.TargetLanguageName,
		&i.UiLanguageID,
		&i.OwnerID,
		&i.IsPublished,
		&i.Visibility,
		&i.InviteCode,
	)
	return i, err
}

const getLessonBlockWithCourse = `-- name: GetLessonBlockWithCourse :one
SELECT lb.id, lb.lesson_id, lb.section_id, lb.sort_order, lb.display_label, lb.title, lb.block_type, lb.config, lb.is_homework,
       l.course_id, l.title AS lesson_title
FROM lesson_blocks lb
JOIN lessons l ON l.id = lb.lesson_id
WHERE lb.id = $1
`

type GetLessonBlockWithCourseRow struct {
	ID           uuid.UUID
	LessonID     uuid.UUID
	SectionID    *uuid.UUID
	SortOrder    int32
	DisplayLabel *string
	Title        *string
	BlockType    string
	Config       []byte
	IsHomework   bool
	CourseID     uuid.UUID
	LessonTitle  string
}

// GetLessonBlockWithCourse
//
//	SELECT lb.id, lb.lesson_id, lb.section_id, lb.sort_order, lb.display_label, lb.title, lb.block_type, lb.config, lb.is_homework,
//	       l.course_id, l.title AS lesson_title
//	FROM lesson_blocks lb
//	JOIN lessons l ON l.id = lb.lesson_id
//	WHERE lb.id = $1
func (q *Queries) GetLessonBlockWithCourse(ctx context.Context, id uuid.UUID) (GetLessonBlockWithCourseRow, error) {
	row := q.db.QueryRow(ctx, getLessonBlockWithCourse, id)
	var i GetLessonBlockWithCourseRow
	err := row.Scan(
		&i.ID,
		&i.LessonID,
		&i.SectionID,
		&i.SortOrder,
		&i.DisplayLabel,
		&i.Title,
		&i.BlockType,
		&i.Config,
		&i.IsHomework,
		&i.CourseID,
		&i.LessonTitle,
	)
	return i, err
}

const getLessonTitle = `-- name: GetLessonTitle :one
SELECT title FROM lessons WHERE id = $1
`

// GetLessonTitle
//
//	SELECT title FROM lessons WHERE id = $1
func (q *Queries) GetLessonTitle(ctx context.Context, id uuid.UUID) (string, error) {
	row := q.db.QueryRow(ctx, getLessonTitle, id)
	var title string
	err := row.Scan(&title)
	return title, err
}

const listPublishedCourses = `-- name: ListPublishedCourses :many
SELECT
    c.id,
    c.title,
    c.target_language_id,
    ''::text AS target_language_code,
    ''::text AS target_language_name,
    c.ui_language_id,
    c.owner_id,
    c.is_published,
    c.visibility
FROM courses c
WHERE c.is_published = true AND c.visibility = 'public'
ORDER BY c.updated_at DESC
`

type ListPublishedCoursesRow struct {
	ID                 uuid.UUID
	Title              string
	TargetLanguageID   uuid.UUID
	TargetLanguageCode string
	TargetLanguageName string
	UiLanguageID       uuid.UUID
	OwnerID            uuid.UUID
	IsPublished        bool
	Visibility         string
}

// ListPublishedCourses
//
//	SELECT
//	    c.id,
//	    c.title,
//	    c.target_language_id,
//	    ''::text AS target_language_code,
//	    ''::text AS target_language_name,
//	    c.ui_language_id,
//	    c.owner_id,
//	    c.is_published,
//	    c.visibility
//	FROM courses c
//	WHERE c.is_published = true AND c.visibility = 'public'
//	ORDER BY c.updated_at DESC
func (q *Queries) ListPublishedCourses(ctx context.Context) ([]ListPublishedCoursesRow, error) {
	rows, err := q.db.Query(ctx, listPublishedCourses)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListPublishedCoursesRow
	for rows.Next() {
		var i ListPublishedCoursesRow
		if err := rows.Scan(
			&i.ID,
			&i.Title,
			&i.TargetLanguageID,
			&i.TargetLanguageCode,
			&i.TargetLanguageName,
			&i.UiLanguageID,
			&i.OwnerID,
			&i.IsPublished,
			&i.Visibility,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listPublishedLessonsByCourse = `-- name: ListPublishedLessonsByCourse :many
SELECT id, course_id, title, sort_order, version, status
FROM lessons
WHERE course_id = $1 AND status = 'published'
ORDER BY sort_order ASC
`

type ListPublishedLessonsByCourseRow struct {
	ID        uuid.UUID
	CourseID  uuid.UUID
	Title     string
	SortOrder int32
	Version   int32
	Status    string
}

// ListPublishedLessonsByCourse
//
//	SELECT id, course_id, title, sort_order, version, status
//	FROM lessons
//	WHERE course_id = $1 AND status = 'published'
//	ORDER BY sort_order ASC
func (q *Queries) ListPublishedLessonsByCourse(ctx context.Context, courseID uuid.UUID) ([]ListPublishedLessonsByCourseRow, error) {
	rows, err := q.db.Query(ctx, listPublishedLessonsByCourse, courseID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListPublishedLessonsByCourseRow
	for rows.Next() {
		var i ListPublishedLessonsByCourseRow
		if err := rows.Scan(
			&i.ID,
			&i.CourseID,
			&i.Title,
			&i.SortOrder,
			&i.Version,
			&i.Status,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
