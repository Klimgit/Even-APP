// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: sections.sql

package query

import (
	"context"

	"github.com/google/uuid"
)

const createSection = `-- name: CreateSection :one
INSERT INTO lesson_sections (lesson_id, title, sort_order, section_kind)
VALUES ($1, $2, $3, $4)
RETURNING id, lesson_id, title, sort_order, section_kind
`

type CreateSectionParams struct {
	LessonID    uuid.UUID
	Title       string
	SortOrder   int32
	SectionKind string
}

// CreateSection
//
//	INSERT INTO lesson_sections (lesson_id, title, sort_order, section_kind)
//	VALUES ($1, $2, $3, $4)
//	RETURNING id, lesson_id, title, sort_order, section_kind
func (q *Queries) CreateSection(ctx context.Context, arg CreateSectionParams) (LessonSection, error) {
	row := q.db.QueryRow(ctx, createSection,
		arg.LessonID,
		arg.Title,
		arg.SortOrder,
		arg.SectionKind,
	)
	var i LessonSection
	err := row.Scan(
		&i.ID,
		&i.LessonID,
		&i.Title,
		&i.SortOrder,
		&i.SectionKind,
	)
	return i, err
}

const deleteSection = `-- name: DeleteSection :exec
DELETE FROM lesson_sections WHERE id = $1
`

// DeleteSection
//
//	DELETE FROM lesson_sections WHERE id = $1
func (q *Queries) DeleteSection(ctx context.Context, id uuid.UUID) error {
	_, err := q.db.Exec(ctx, deleteSection, id)
	return err
}

const getSectionByID = `-- name: GetSectionByID :one
SELECT id, lesson_id, title, sort_order, section_kind
FROM lesson_sections
WHERE id = $1
`

// GetSectionByID
//
//	SELECT id, lesson_id, title, sort_order, section_kind
//	FROM lesson_sections
//	WHERE id = $1
func (q *Queries) GetSectionByID(ctx context.Context, id uuid.UUID) (LessonSection, error) {
	row := q.db.QueryRow(ctx, getSectionByID, id)
	var i LessonSection
	err := row.Scan(
		&i.ID,
		&i.LessonID,
		&i.Title,
		&i.SortOrder,
		&i.SectionKind,
	)
	return i, err
}

const getSectionCourseOwner = `-- name: GetSectionCourseOwner :one
SELECT c.owner_id
FROM lesson_sections s
JOIN lessons l ON l.id = s.lesson_id
JOIN courses c ON c.id = l.course_id
WHERE s.id = $1
`

// GetSectionCourseOwner
//
//	SELECT c.owner_id
//	FROM lesson_sections s
//	JOIN lessons l ON l.id = s.lesson_id
//	JOIN courses c ON c.id = l.course_id
//	WHERE s.id = $1
func (q *Queries) GetSectionCourseOwner(ctx context.Context, id uuid.UUID) (uuid.UUID, error) {
	row := q.db.QueryRow(ctx, getSectionCourseOwner, id)
	var owner_id uuid.UUID
	err := row.Scan(&owner_id)
	return owner_id, err
}

const listSectionsByLessonID = `-- name: ListSectionsByLessonID :many
SELECT id, lesson_id, title, sort_order, section_kind
FROM lesson_sections
WHERE lesson_id = $1
ORDER BY sort_order, title
`

// ListSectionsByLessonID
//
//	SELECT id, lesson_id, title, sort_order, section_kind
//	FROM lesson_sections
//	WHERE lesson_id = $1
//	ORDER BY sort_order, title
func (q *Queries) ListSectionsByLessonID(ctx context.Context, lessonID uuid.UUID) ([]LessonSection, error) {
	rows, err := q.db.Query(ctx, listSectionsByLessonID, lessonID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []LessonSection
	for rows.Next() {
		var i LessonSection
		if err := rows.Scan(
			&i.ID,
			&i.LessonID,
			&i.Title,
			&i.SortOrder,
			&i.SectionKind,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const maxSectionSortOrder = `-- name: MaxSectionSortOrder :one
SELECT COALESCE(MAX(sort_order), -1)::int AS max_sort
FROM lesson_sections
WHERE lesson_id = $1
`

// MaxSectionSortOrder
//
//	SELECT COALESCE(MAX(sort_order), -1)::int AS max_sort
//	FROM lesson_sections
//	WHERE lesson_id = $1
func (q *Queries) MaxSectionSortOrder(ctx context.Context, lessonID uuid.UUID) (int32, error) {
	row := q.db.QueryRow(ctx, maxSectionSortOrder, lessonID)
	var max_sort int32
	err := row.Scan(&max_sort)
	return max_sort, err
}

const setSectionSortOrder = `-- name: SetSectionSortOrder :exec
UPDATE lesson_sections SET sort_order = $2 WHERE id = $1
`

type SetSectionSortOrderParams struct {
	ID        uuid.UUID
	SortOrder int32
}

// SetSectionSortOrder
//
//	UPDATE lesson_sections SET sort_order = $2 WHERE id = $1
func (q *Queries) SetSectionSortOrder(ctx context.Context, arg SetSectionSortOrderParams) error {
	_, err := q.db.Exec(ctx, setSectionSortOrder, arg.ID, arg.SortOrder)
	return err
}

const updateSection = `-- name: UpdateSection :one
UPDATE lesson_sections
SET
    title = COALESCE($1, title),
    sort_order = COALESCE($2, sort_order),
    section_kind = COALESCE($3, section_kind)
WHERE id = $4
RETURNING id, lesson_id, title, sort_order, section_kind
`

type UpdateSectionParams struct {
	Title       *string
	SortOrder   *int32
	SectionKind *string
	ID          uuid.UUID
}

// UpdateSection
//
//	UPDATE lesson_sections
//	SET
//	    title = COALESCE($1, title),
//	    sort_order = COALESCE($2, sort_order),
//	    section_kind = COALESCE($3, section_kind)
//	WHERE id = $4
//	RETURNING id, lesson_id, title, sort_order, section_kind
func (q *Queries) UpdateSection(ctx context.Context, arg UpdateSectionParams) (LessonSection, error) {
	row := q.db.QueryRow(ctx, updateSection,
		arg.Title,
		arg.SortOrder,
		arg.SectionKind,
		arg.ID,
	)
	var i LessonSection
	err := row.Scan(
		&i.ID,
		&i.LessonID,
		&i.Title,
		&i.SortOrder,
		&i.SectionKind,
	)
	return i, err
}
