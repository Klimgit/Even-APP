openapi: 3.0.3
info:
  title: even-lexicon
  version: 0.2.0
  description: Languages, lexicon, and vocabulary (media lives in the media service)

paths:
  /api/v1/health:
    get:
      operationId: getHealth
      tags: [System, Public]
      summary: Health check
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"

components:
  schemas:
    HealthResponse:
      type: object
      required: [status, service]
      properties:
        status:
          type: string
        service:
          type: string
