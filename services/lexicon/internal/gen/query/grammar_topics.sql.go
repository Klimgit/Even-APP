// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: grammar_topics.sql

package query

import (
	"context"

	"github.com/google/uuid"
)

const createGrammarTopic = `-- name: CreateGrammarTopic :one
INSERT INTO grammar_topics (language_id, title, description, sort_order)
VALUES ($1, $2, $3, COALESCE($4, 0))
RETURNING id, language_id, title, description, sort_order, created_at, updated_at
`

type CreateGrammarTopicParams struct {
	LanguageID  uuid.UUID
	Title       string
	Description string
	SortOrder   interface{}
}

// CreateGrammarTopic
//
//	INSERT INTO grammar_topics (language_id, title, description, sort_order)
//	VALUES ($1, $2, $3, COALESCE($4, 0))
//	RETURNING id, language_id, title, description, sort_order, created_at, updated_at
func (q *Queries) CreateGrammarTopic(ctx context.Context, arg CreateGrammarTopicParams) (GrammarTopic, error) {
	row := q.db.QueryRow(ctx, createGrammarTopic,
		arg.LanguageID,
		arg.Title,
		arg.Description,
		arg.SortOrder,
	)
	var i GrammarTopic
	err := row.Scan(
		&i.ID,
		&i.LanguageID,
		&i.Title,
		&i.Description,
		&i.SortOrder,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const deleteGrammarTopic = `-- name: DeleteGrammarTopic :exec
DELETE FROM grammar_topics WHERE id = $1
`

type DeleteGrammarTopicParams struct {
	ID uuid.UUID
}

// DeleteGrammarTopic
//
//	DELETE FROM grammar_topics WHERE id = $1
func (q *Queries) DeleteGrammarTopic(ctx context.Context, arg DeleteGrammarTopicParams) error {
	_, err := q.db.Exec(ctx, deleteGrammarTopic, arg.ID)
	return err
}

const listGrammarTopicsByLanguageCode = `-- name: ListGrammarTopicsByLanguageCode :many
SELECT gt.id, gt.language_id, gt.title, gt.description, gt.sort_order, gt.created_at, gt.updated_at
FROM grammar_topics gt
JOIN languages l ON l.id = gt.language_id
WHERE l.code = $1
ORDER BY gt.sort_order, gt.title
`

type ListGrammarTopicsByLanguageCodeParams struct {
	Code string
}

// ListGrammarTopicsByLanguageCode
//
//	SELECT gt.id, gt.language_id, gt.title, gt.description, gt.sort_order, gt.created_at, gt.updated_at
//	FROM grammar_topics gt
//	JOIN languages l ON l.id = gt.language_id
//	WHERE l.code = $1
//	ORDER BY gt.sort_order, gt.title
func (q *Queries) ListGrammarTopicsByLanguageCode(ctx context.Context, arg ListGrammarTopicsByLanguageCodeParams) ([]GrammarTopic, error) {
	rows, err := q.db.Query(ctx, listGrammarTopicsByLanguageCode, arg.Code)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []GrammarTopic
	for rows.Next() {
		var i GrammarTopic
		if err := rows.Scan(
			&i.ID,
			&i.LanguageID,
			&i.Title,
			&i.Description,
			&i.SortOrder,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const patchGrammarTopic = `-- name: PatchGrammarTopic :one
UPDATE grammar_topics
SET
    title = COALESCE($1, title),
    description = COALESCE($2, description),
    sort_order = COALESCE($3, sort_order),
    updated_at = now()
WHERE id = $4
RETURNING id, language_id, title, description, sort_order, created_at, updated_at
`

type PatchGrammarTopicParams struct {
	Title       *string
	Description *string
	SortOrder   *int32
	ID          uuid.UUID
}

// PatchGrammarTopic
//
//	UPDATE grammar_topics
//	SET
//	    title = COALESCE($1, title),
//	    description = COALESCE($2, description),
//	    sort_order = COALESCE($3, sort_order),
//	    updated_at = now()
//	WHERE id = $4
//	RETURNING id, language_id, title, description, sort_order, created_at, updated_at
func (q *Queries) PatchGrammarTopic(ctx context.Context, arg PatchGrammarTopicParams) (GrammarTopic, error) {
	row := q.db.QueryRow(ctx, patchGrammarTopic,
		arg.Title,
		arg.Description,
		arg.SortOrder,
		arg.ID,
	)
	var i GrammarTopic
	err := row.Scan(
		&i.ID,
		&i.LanguageID,
		&i.Title,
		&i.Description,
		&i.SortOrder,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
