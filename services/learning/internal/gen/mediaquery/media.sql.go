// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: media.sql

package mediaquery

import (
	"context"

	"github.com/google/uuid"
)

const listMediaByIDs = `-- name: ListMediaByIDs :many
SELECT id, scope, display_name, mime_type, media_kind
FROM media_assets
WHERE id = ANY($1::uuid[])
  AND mime_type != 'application/octet-stream'
  AND (expires_at IS NULL OR expires_at > now())
`

type ListMediaByIDsParams struct {
	Column1 []uuid.UUID
}

type ListMediaByIDsRow struct {
	ID          uuid.UUID
	Scope       string
	DisplayName string
	MimeType    string
	MediaKind   string
}

// ListMediaByIDs
//
//	SELECT id, scope, display_name, mime_type, media_kind
//	FROM media_assets
//	WHERE id = ANY($1::uuid[])
//	  AND mime_type != 'application/octet-stream'
//	  AND (expires_at IS NULL OR expires_at > now())
func (q *Queries) ListMediaByIDs(ctx context.Context, arg ListMediaByIDsParams) ([]ListMediaByIDsRow, error) {
	rows, err := q.db.Query(ctx, listMediaByIDs, arg.Column1)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListMediaByIDsRow
	for rows.Next() {
		var i ListMediaByIDsRow
		if err := rows.Scan(
			&i.ID,
			&i.Scope,
			&i.DisplayName,
			&i.MimeType,
			&i.MediaKind,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
